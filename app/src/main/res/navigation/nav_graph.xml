<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.gamevault.app.ui.home.HomeFragment"
        android:label="Home">
        <action
            android:id="@+id/action_home_to_detail"
            app:destination="@id/detailFragment">
            <argument
                android:name="gameId"
                app:argType="integer" />
        </action>
    </fragment>

    <fragment
        android:id="@+id/searchFragment"
        android:name="com.gamevault.app.ui.search.SearchFragment"
        android:label="Search">
        <action
            android:id="@+id/action_search_to_detail"
            app:destination="@id/detailFragment">
            <argument
                android:name="gameId"
                app:argType="integer" />
        </action>
    </fragment>

    <fragment
        android:id="@+id/favoritesFragment"
        android:name="com.gamevault.app.ui.favorites.FavoritesFragment"
        android:label="Favorites">
        <action
            android:id="@+id/action_favorites_to_detail"
            app:destination="@id/detailFragment">
            <argument
                android:name="gameId"
                app:argType="integer" />
        </action>
    </fragment>

    <fragment
        android:id="@+id/detailFragment"
        android:name="com.gamevault.app.ui.detail.DetailFragment"
        android:label="Detail">
        <argument
            android:name="gameId"
            app:argType="integer" />
    </fragment>

</navigation>
